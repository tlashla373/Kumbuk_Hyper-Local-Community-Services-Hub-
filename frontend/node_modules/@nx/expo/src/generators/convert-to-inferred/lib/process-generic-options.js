"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processGenericOptions = processGenericOptions;
const devkit_1 = require("@nx/devkit");
function processGenericOptions(_tree, options, _projectName, _projectRoot, _migrationLogs) {
    const args = options.args ?? [];
    for (const key of Object.keys(options)) {
        if (key === 'args')
            continue;
        let value = options[key];
        if (typeof value === 'boolean') {
            if (value)
                args.push(`--${(0, devkit_1.names)(key).fileName}`);
        }
        else {
            args.push(`--${(0, devkit_1.names)(key).fileName}=${value}`);
        }
        delete options[key];
    }
    options.args = args;
}
